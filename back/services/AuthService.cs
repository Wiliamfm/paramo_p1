namespace paramo.services;

/*
static async Task<IResult> Login(string email, string password)
{
  if(email == "admin@test.com")
  {
    return Results.Ok(new { token = "token-secret" });
  }
  return Results.Unauthorized();
}

static async Task<IResult> Logout()
{
  //TODO: Use memory to keep track of active tokens, remove it when logout or the expires?
}
*/
