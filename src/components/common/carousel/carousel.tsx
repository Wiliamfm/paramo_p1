import { component$ } from "@builder.io/qwik"


interface CardProps {
    ImgUrl: string;
    title: string;
    description?: string;
    url?: string;
  }

export default component$<CardProps>(() => {
    return (        
        <div >
    
        </div>
    );

});
